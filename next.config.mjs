/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "randomuser.me",
      "cdn.pixabay.com",
      "picsum.photos",
      "res.cloudinary.com",
      "cdn.sanity.io",
    ],
  },
};

export default nextConfig;
